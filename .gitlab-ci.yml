image: python:3.12-slim

stages:
  - test

test:
  stage: test
  script:
    - python -m pip install -e .
    - python -m compileall src
    - python -m unittest discover -s tests
    - london-property-analysis --help
